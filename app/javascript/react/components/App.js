import React from 'react'
import { BrowserRouter, Switch } from "react-router-dom"

export const App = (props) => {
  return (
    <BrowserRouter>
      <Switch> 
      </Switch>
    </BrowserRouter>
  )
}

export default App
